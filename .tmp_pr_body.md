## Resumen

Este PR corrige el flujo de elegibilidad profesional y validaci�n de `/pro-apply` para que:

- una postulaci�n `pending` no habilite capacidades de profesional
- la activaci�n real dependa de aprobaci�n admin y `professionals.active`
- se eliminen l�mites artificiales en categor�as/subcategor�as del formulario

## Cambios clave

- `/pro-apply` frontend y backend:
  - se removieron topes `max(20)` en `categories` y `max(50)` en `subcategories`
  - se mantiene validaci�n m�nima (al menos 1)
- `app/api/pro-applications/route.ts`:
  - se elimin� el write prematuro que marcaba `profiles.is_professional=true` al enviar solicitud `pending`
- `app/api/admin/pro-applications/[id]/status/route.ts`:
  - al `accepted`: sincroniza `profiles.role="pro"` e `is_client_pro=true`
  - al `rejected|pending`: desactiva `professionals.active=false` y baja `profiles` a `role="client", is_client_pro=false`
- Guards de elegibilidad reforzados:
  - `active-user-type`, `can-switch`, `switch-active-user-type`
  - endpoints de `bank-accounts`
  - `middleware` y `site-header`
  - p�ginas `landing`, `/pro`, `/requests/explore`, `settings`, `profile/setup`

## Validaci�n

- `npm run lint` ?
- `npm run typecheck` ?? falla por errores preexistentes del repo fuera de este alcance.

## Checklist manual sugerida

1. Postulaci�n `pending` no habilita UX pro ni switch a profesional.
2. Aceptaci�n admin habilita profesional (`professionals.active=true`, `role=pro`, `is_client_pro=true`).
3. Rechazo admin no habilita profesional.
4. Con `professionals.active=false`, `/pro` y capacidades pro quedan bloqueadas.
5. En `/pro-apply`, se pueden seleccionar todas las categor�as/subcategor�as disponibles sin tope artificial.
